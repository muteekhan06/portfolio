# ADMIN AUTHENTICATION (FREE & SECURE)
# Generate password hash: node -e "const bcrypt = require('bcryptjs'); console.log(bcrypt.hashSync('YourStrongPassword', 10));"
VITE_ADMIN_PASSWORD_HASH=$2a$10$EXAMPLEHASHREPLACEWITHYOUROWN

# Generate TOTP secret: node -e "const speakeasy = require('speakeasy'); console.log(speakeasy.generateSecret().base32);"
VITE_ADMIN_TOTP_SECRET=EXAMPLESECRETREPLACEWITHYOUROWN

# GitHub Personal Access Token (for committing data changes)
# Scope: repo (full control of private repositories)
# Generate at: https://github.com/settings/tokens
VITE_GITHUB_PAT=ghp_YOURTOKEN
VITE_GITHUB_REPO=yourusername/yourrepo
VITE_GITHUB_BRANCH=main

# Email sending: local queue is used by default (EmailJS removed)
# If you want a third-party provider, update contact logic and this file accordingly.

# JWT Secret (for admin sessions - generate random string)
VITE_JWT_SECRET=your-super-secret-jwt-key-change-this

# Optional: Plausible Analytics (self-hosted, free)
# VITE_PLAUSIBLE_DOMAIN=yourdomain.com
# VITE_PLAUSIBLE_API_HOST=https://plausible.io
