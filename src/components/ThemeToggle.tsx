// Theme toggle removed — component left as a harmless stub to avoid accidental imports.
// The site is intentionally dark-only. Remove this file if you want it gone from disk.

// Theme toggle removed — this file is retained as a harmless compatibility stub.
// The site defaults to dark mode. Feel free to delete this file locally and commit the removal.

export function ThemeToggle() {
  // No-op: theme toggling intentionally disabled.
  return null;
}
